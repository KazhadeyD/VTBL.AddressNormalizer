# Каталог: VTBL.AddressNormalizer.WebApi/Filters

## Назначение
MVC filters (global).

## Файлы

### ApiExceptionFilter.cs
**Классы:**
- `ApiExceptionFilter` — unhandled → HTTP 500 + `{ "error": "<message>" }`, лог Error
  - `OnException(ExceptionContext)` — обработка исключения; Correlation Id из MDLC через layout NLog
  - не вмешивается в per-item batch (такие ошибки не вылетают как unhandled)

## Зависимости
- `Models/ErrorResponse`
- `Microsoft.Extensions.Logging`
