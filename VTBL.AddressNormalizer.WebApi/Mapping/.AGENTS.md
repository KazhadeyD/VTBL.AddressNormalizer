# Каталог: VTBL.AddressNormalizer.WebApi/Mapping

## Назначение
Маппинг моделей ядра в HTTP DTO (вариант B indoor_value).

## Файлы

### IndoorValueMapper.cs
**Классы:**
- `IndoorValueMapper` (static)
  - `ToVariantB(BuildingUnitLocation)` — полный `IndoorValueDto`: все 17 свойств; `Name` из `CategoryNames`; `Values` = копия коллекций Location (пустые → `[]`); `null` location → пустые категории
  - `CategoryNames` — константы русских имён (ТЗ 2.A / architecture §4.2)

## Зависимости
- `VTBL.AddressNormalizer.Abstractions.BuildingUnit`
- `VTBL.AddressNormalizer.WebApi.Models`
