# Каталог: VTBL.AddressNormalizer.UnitTests/BuildingAddress

## Назначение
Unit/E2E-тесты ядра BuildingAddress: extract, canonicalize, normalize.

## Файлы

### BuildingLocationExtractorSplitTests.cs
**Классы:**
- `BuildingLocationExtractorSplitTests` — `ExtractSplit`
  - сценарии outdoor + indoor (#1–#3)
  - indoor до дома → Outdoor=`""`
  - comma-before-marker → Indoor без ведущей `,`
  - Indoor never null
  - `Extract(x) == ExtractSplit(x).Outdoor`

### BuildingAddressCoreTests.cs
**Классы:**
- `BuildingLocationExtractorTests` — регрессия `Extract` (outdoor) + smoke ExtractSplit
- `BuildingAddressCanonicalizerTests` — `ToCanonical`
- `BuildingAddressNormalizerTests` — E2E Normalize (включая ExtractSplit Indoor)
- `IndoorMarkerRegistryTests` — leftmost marker / false positive «п»

### BuildingLocationExtractionResultTests.cs
**Классы:**
- `BuildingLocationExtractionResultTests` — null → `""` для Outdoor/Indoor

## Зависимости
- `VTBL.AddressNormalizer.Abstractions`
- `VTBL.AddressNormalizer.Infrastructure` (`AddressNormalizerFactory`)
