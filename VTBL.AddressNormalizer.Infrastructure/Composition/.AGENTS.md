# Каталог: VTBL.AddressNormalizer.Infrastructure/Composition

## Назначение
Composition root ядра через MS.DI: `AddAddressNormalizer`.

## Файлы

### AddressNormalizerServiceCollectionExtensions.cs
**Классы:**
- `AddressNormalizerServiceCollectionExtensions`
  - `AddAddressNormalizer(IServiceCollection)` — регистрация singleton-графа ядра (WebApi, Console, тесты);
    `TryAddSingleton<ILogger>(NullLogger)` если хост ещё не зарегистрировал логгер

## Зависимости
- `Microsoft.Extensions.DependencyInjection.Abstractions`
- `Abstractions.Logging.ILogger`
- реализации BuildingAddress / BuildingUnit / Shared
