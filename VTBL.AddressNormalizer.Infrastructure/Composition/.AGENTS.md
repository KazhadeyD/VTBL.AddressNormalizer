# Каталог: VTBL.AddressNormalizer.Infrastructure/Composition

## Назначение
Composition root ядра: без DI (`AddressNormalizerFactory`) и с DI (`AddAddressNormalizer`).

## Файлы

### AddressNormalizerFactory.cs
**Классы:**
- `AddressNormalizerFactory` (static) — lazy singleton для Console и тестов без контейнера

### AddressNormalizerServiceCollectionExtensions.cs
**Классы:**
- `AddressNormalizerServiceCollectionExtensions`
  - `AddAddressNormalizer(IServiceCollection)` — регистрация того же графа как singleton в MS.DI (WebApi и др.)

## Зависимости
- `Microsoft.Extensions.DependencyInjection.Abstractions`
- реализации BuildingAddress / BuildingUnit / Shared / FieldAdapters
